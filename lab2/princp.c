#include <stdio.h>
#include <stdlib.h>
#include "VetoresC.c"

int main(){

    int ptammax;
    printf("Digite a quantidade de elementos a serem alocados: ");
    scanf("%d", &ptammax);
    if (ptammax <= 0) exit(1);
    char pvetcar[ptammax];
}